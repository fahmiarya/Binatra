<!-- <script setup>
import socket from '@/socket';

socket.on('device-setting', () => {

});

const handleSubmit = () => {
    socket.emit('device-setting', {
    deviceCode : 'DEV005',
    calibration : 60,
    periode : 40
  })
}
</script>


<template>
  <button class="bg-sky-500 border px-3 py-2" @click="handleSubmit">send</button>
</template> -->
